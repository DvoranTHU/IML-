# 《机器学习概论》大实验申请

**题目：基于多视角声学特征与生成-判别混合模型的语音情感识别研究**

文宇浩 2023010832 计36

## 一、选题背景与意义

语音情感识别旨在仅凭声学信号判断说话人所表达的情感类别，是人机交互、心理健康监测、智能客服与教育评估等应用的关键技术。相较于 LAB1（文本二分类）、LAB2（MNIST 像素聚类）与 LAB3（文本评分预测）所处理的离散文本与静态像素，语音是高维、连续、含时序结构且受说话人个体差异显著影响的信号，难度更高，更能检验机器学习方法的特征建模能力。深度学习兴起前，SER 的经典最优方法是「声学特征工程 + 生成式 GMM/HMM + 判别式核 SVM/GBDT + 集成融合」。本课题将系统比较该生成式与判别式范式，并通过跨语料实验考察域迁移鲁棒性。

## 二、拟采用的特征与算法

### (1) 多视角声学特征

采用帧级特征（25 ms 窗、10 ms 步长）：13 维 MFCC 及其一阶、二阶差分（Δ、ΔΔ）共 39 维倒谱特征；F0（基频）、能量、过零率、谱质心、谱滚降、谱带宽、谐噪比 HNR、抖动 jitter 与微颤 shimmer 等共约 20 维韵律与音质特征。再用 8 种函数统计量（mean / std / min / max / skew / kurtosis / 25-75 分位 / 线性回归斜率）在话语级聚合，得到约 470 维定长向量，近似复现 INTERSPEECH-2009 ComParE 与 GeMAPS 风格的特征集。

### (2) 生成式模型 GMM-MAP

为每一类情感独立训练一个高斯混合模型（K ∈ {8, 16, 32, 64} 个分量，对角协方差，EM 迭代直至对数似然收敛），分类时按 argmax_y log p(x|y) + log π_y 给出最大后验类。这是 LAB2 KMeans（硬分配、单中心）的概率化推广，建模软分配与协方差结构。

### (3) 判别式模型 核 SVM

采用 one-vs-rest 多分类核 SVM，对比线性、RBF、多项式核；用 5 折网格搜索 (C, γ)；特征前置 z-score 标准化，必要时辅以 Kernel-PCA 降维到 80–150 维以加速训练。

### (4) 集成与序列扩展

梯度提升决策树（GBDT，LightGBM 实现）作为前向加性树模型，扫描树数 100–1000、深度 4–8、学习率 0.01–0.1；同时构建 GMM-HMM（每情感一条 HMM，3–5 状态，GMM 发射，Baum-Welch 训练）以利用帧级时序信息，与话语级模型对比。

### (5) Stacking 元学习融合

将 GMM-MAP、核 SVM、GBDT、HMM 的类别概率作为元特征，用 Logistic Regression 做元学习器，5 折 out-of-fold 训练以避免泄漏，最终输出融合预测。

## 三、数据与实验方案

**数据集：**主数据集 RAVDESS（24 位演员，8 类情感：neutral / calm / happy / sad / angry / fearful / disgust / surprised，1440 条英文音频，公开免费）；辅助数据集 CREMA-D（91 位演员，6 类，7442 条，公开）；跨语料测试 EMODB（10 位德语演员，7 类，535 条）。三者均为 SER 领域标准基准。所有音频统一重采样至 16 kHz、单声道、归一化幅度。

**协议：**采用 Speaker-Independent 评估（按演员划分）。RAVDESS 上做 5 折 leave-speakers-out 交叉验证（每折 19/20 训练 + 4/5 测试），CREMA-D 上做 80/20 演员分层划分，重复 3 次取均值。跨语料协议：在 RAVDESS 上训练，直接在 CREMA-D / EMODB 共同情感子集（angry / happy / sad / neutral / fearful）上做 zero-shot 评估，衡量域偏移下的鲁棒性。

**实验组：**

1. 单模型基线：GMM-MAP / 核 SVM / GBDT / GMM-HMM 各自最优配置
2. 模型对比与消融：去掉 Δ-MFCC、去掉韵律、去掉函数统计，量化各特征视角贡献
3. Stacking 融合
4. 跨语料评估
5. PCA / Kernel-PCA + t-SNE 可视化情感簇分布并对照混淆矩阵

## 四、评价方案与预期结果

**评价指标：**Accuracy、UAR（Unweighted Average Recall，对类不平衡更敏感，SER 领域主指标）、Macro-F1、类别混淆矩阵，以及训练 + 推理总耗时。所有 metric 给出 95% 置信区间，使用配对 t 检验比较不同模型在同一折上的差异显著性。

**预期结果：**

1. RAVDESS 上单模型 UAR 大致为 GMM 55%、核 SVM 60%、GBDT 62%、GMM-HMM 58%
2. Stacking 融合将 UAR 提升至约 65–68%，与文献中传统方法上限相当
3. 韵律特征对高/低唤起度情感的区分贡献显著，去掉后 UAR 下降 3–5 个百分点
4. 跨语料 RAVDESS→CREMA-D 的 UAR 较同语料下降 8–15 个百分点，RAVDESS→EMODB 下降更大，刻画域偏移代价
5. Kernel-PCA + t-SNE 可视化中，happy/surprised 与 sad/neutral 形成可识别紧凑簇，但同唤起度类间存在重叠，与混淆矩阵中的错分模式一致

## 五、主要参考文献与数据集

[1] Schuller B, Steidl S, Batliner A. The INTERSPEECH 2009 Emotion Challenge. INTERSPEECH 2009: 312–315.

[2] Eyben F, Scherer K R, Schuller B, et al. The Geneva Minimalistic Acoustic Parameter Set (GeMAPS) for Voice Research and Affective Computing. IEEE TAC, 2016, 7(2): 190–202.

[3] Reynolds D A, Rose R C. Robust Text-Independent Speaker Identification Using Gaussian Mixture Speaker Models. IEEE TSAP, 1995, 3(1): 72–83.
